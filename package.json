{
  "name": "session-proof-scraper",
  "version": "1.0.0",
  "description": "Playwright scraper backend for Session Proof Checker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "^1.45.3",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}
